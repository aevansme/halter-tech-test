import React from 'react'

const OtherPage = () => {
    return (
        <div>
            {/* Doesn't exist because this is a sandbox app :) .. However other pages
                like this would be structured beneath the pages folder */}
        </div>
    )
}

export default OtherPage
