import {render, screen} from '@testing-library/react-native'
import BinaryControl from "../BinaryControl"

it('should render the binary options on the page', async () => {
    // TODO:... Unit Tests using RNTL
})

it('should toggle the selected option when the option is pressed', async () => {
    // TODO:... Unit Tests using RNTL
})